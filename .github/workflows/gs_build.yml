name: Ground Station Build
on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    
    steps:
    - uses: actions/checkout@v3
        
    - name: Build
      run: |
        make -f ground_station/Makefile

name: Test
on:
  pull_request:
  push:
    branches:
      - main
      - dangle/force-obc-tests-compile-32-bits

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install gcc-multilib
      run: sudo apt-get update && sudo apt-get install -y gcc-multilib
    - name: Configure CMake
      run: |
        mkdir build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=Test
    - name: Build
      run: |
        cd build
        cmake --build .
    - name: Run Tests
      run: |
        cd build
        ctest --verbose
